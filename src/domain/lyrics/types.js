/**
 * @typedef {Readonly<{
 *   timeMs: number,
 *   text: string,
 * }>} LyricLine
 *
 * @typedef {Readonly<{
 *   artist?: string | null,
 *   title?: string | null,
 *   album?: string | null,
 *   durationMs?: number | null,
 * }>} TrackMetadataInput
 *
 * @typedef {Readonly<{
 *   artist: string,
 *   title: string,
 *   album: string,
 *   durationMs: number | null,
 * }>} LyricsQuery
 */

export {};
